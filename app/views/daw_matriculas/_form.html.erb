<%= form_for(@daw_matricula) do |f| %>
  <% if @daw_matricula.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daw_matricula.errors.count, "error") %> prohibited this daw_matricula from being saved:</h2>

      <ul>
      <% @daw_matricula.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
