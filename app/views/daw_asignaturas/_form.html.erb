<%= form_for(@daw_asignatura) do |f| %>
  <% if @daw_asignatura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daw_asignatura.errors.count, "error") %> prohibited this daw_asignatura from being saved:</h2>

      <ul>
      <% @daw_asignatura.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
