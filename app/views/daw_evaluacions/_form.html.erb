<%= form_for(@daw_evaluacion) do |f| %>
  <% if @daw_evaluacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daw_evaluacion.errors.count, "error") %> prohibited this daw_evaluacion from being saved:</h2>

      <ul>
      <% @daw_evaluacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
