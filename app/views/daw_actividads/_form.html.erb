<%= form_for(@daw_actividad) do |f| %>
  <% if @daw_actividad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daw_actividad.errors.count, "error") %> prohibited this daw_actividad from being saved:</h2>

      <ul>
      <% @daw_actividad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
