<%= form_for(@daw_opcion) do |f| %>
  <% if @daw_opcion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daw_opcion.errors.count, "error") %> prohibited this daw_opcion from being saved:</h2>

      <ul>
      <% @daw_opcion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
